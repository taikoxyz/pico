# Hub StatefulSet (app + litestream sidecar), Service, public Ingress, and
# GCE-managed TLS certificate. Single replica with a 10Gi PVC for SQLite.
#
# Public surface: HTTPS on the Ingress's external IP (point your DNS at it).
# Metrics surface: 9090 inside the pod, bound to :: for in-cluster Prometheus
# scraping.
#
# Required Secret: `pico-hub-secrets` (set via secrets-bootstrap.sh).

apiVersion: v1
kind: ServiceAccount
metadata:
  name: pico-hub
  namespace: pico
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pico-hub-config
  namespace: pico
data:
  PORT: "3030"
  LOG_LEVEL: "info"
  DB_DRIVER: "sqlite"
  DB_URL: "/data/hub.sqlite"
  PROMETHEUS_PORT: "9090"
  METRICS_BIND_ADDR: "::"
  HUB_REQUIRE_SIGNED_ENVELOPE: "true"
  CHAIN_ID: "167000"
  NODE_ENV: "production"
---
# Litestream config mounted into the sidecar at /etc/litestream.yml.
# Same shape as infra/litestream/hub.yml. ${LITESTREAM_*} resolve from
# the Secret at runtime.
apiVersion: v1
kind: ConfigMap
metadata:
  name: pico-hub-litestream
  namespace: pico
data:
  litestream.yml: |
    dbs:
      - path: /data/hub.sqlite
        replicas:
          - type: s3
            bucket: ${LITESTREAM_R2_BUCKET}
            path: hub.sqlite
            endpoint: ${LITESTREAM_R2_ENDPOINT}
            region: ${LITESTREAM_R2_REGION:-auto}
            access-key-id: ${LITESTREAM_ACCESS_KEY_ID}
            secret-access-key: ${LITESTREAM_SECRET_ACCESS_KEY}
            retention: 720h
            snapshot-interval: 1h
            sync-interval: 10s
---
# Headless Service so the StatefulSet pod has stable DNS for in-cluster
# Prometheus scraping (pico-hub-0.pico-hub.pico.svc).
apiVersion: v1
kind: Service
metadata:
  name: pico-hub
  namespace: pico
  labels:
    app.kubernetes.io/name: pico-hub
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: pico-hub
  ports:
    - name: http
      port: 3030
      targetPort: 3030
    - name: metrics
      port: 9090
      targetPort: 9090
---
# Routable Service in front of the Ingress. ClusterIP is correct here;
# Ingress + GCE LB do the public exposure.
apiVersion: v1
kind: Service
metadata:
  name: pico-hub-ingress
  namespace: pico
  annotations:
    cloud.google.com/backend-config: '{"default": "pico-hub-backend"}'
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pico-hub
  ports:
    - name: http
      port: 80
      targetPort: 3030
---
# Tells the GCE LB to use /v1/health for backend health checks instead of
# the default `/`. Without this the LB will mark the backend unhealthy.
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: pico-hub-backend
  namespace: pico
spec:
  healthCheck:
    type: HTTP
    requestPath: /v1/health
    port: 3030
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 3
  timeoutSec: 60
---
# HTTP -> HTTPS redirect at the GCE LB.
apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: pico-hub-frontend
  namespace: pico
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
---
apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: pico-hub-cert
  namespace: pico
spec:
  domains:
    - pico.taiko.xyz
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pico-hub
  namespace: pico
  annotations:
    kubernetes.io/ingress.class: gce
    kubernetes.io/ingress.global-static-ip-name: pico-hub-ip
    networking.gke.io/managed-certificates: pico-hub-cert
    networking.gke.io/v1beta1.FrontendConfig: pico-hub-frontend
spec:
  defaultBackend:
    service:
      name: pico-hub-ingress
      port:
        number: 80
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pico-hub
  namespace: pico
  labels:
    app.kubernetes.io/name: pico-hub
spec:
  serviceName: pico-hub
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: pico-hub
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pico-hub
    spec:
      serviceAccountName: pico-hub
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      containers:
        - name: hub
          # Replace IMAGE with your Artifact Registry path. The image must be
          # built from apps/hub/Dockerfile.
          image: REGION-docker.pkg.dev/PROJECT/pico/hub:VERSION
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3030
            - name: metrics
              containerPort: 9090
          envFrom:
            - configMapRef:
                name: pico-hub-config
            - secretRef:
                name: pico-hub-secrets
          readinessProbe:
            httpGet:
              path: /v1/health
              port: 3030
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 3030
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
              ephemeral-storage: 1Gi
            limits:
              cpu: 250m
              memory: 512Mi
              ephemeral-storage: 1Gi
          volumeMounts:
            - name: data
              mountPath: /data
        - name: litestream
          image: litestream/litestream:0.3.13
          args: ["replicate", "-config", "/etc/litestream.yml"]
          envFrom:
            - secretRef:
                name: pico-hub-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
              ephemeral-storage: 256Mi
            limits:
              cpu: 100m
              memory: 128Mi
              ephemeral-storage: 256Mi
          volumeMounts:
            - name: data
              mountPath: /data
              readOnly: true
            - name: litestream-config
              mountPath: /etc/litestream.yml
              subPath: litestream.yml
              readOnly: true
      volumes:
        - name: litestream-config
          configMap:
            name: pico-hub-litestream
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ReadWriteOnce]
        resources:
          requests:
            storage: 10Gi
        # Autopilot default StorageClass = standard-rwo (pd-balanced).
        # Omit storageClassName to use it; set explicitly to override.
