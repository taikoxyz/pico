import { describe, expect, it } from 'vitest';

describe.skip('e2e — full payment lifecycle', () => {
  it('open → pay → close (cooperative)', async () => {
    expect(true).toBe(true);
  });

  it('dispute window: counterparty publishes old state, watchtower penalizes', async () => {
    expect(true).toBe(true);
  });

  it('hub-down recovery: client reconnects through alternate hub', async () => {
    expect(true).toBe(true);
  });
});

describe('e2e — bootstrap smoke', () => {
  it('placeholder until anvil + deploy harness lands', () => {
    expect(true).toBe(true);
  });
});
