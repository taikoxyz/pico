services:
  hub:
    build:
      context: ../..
      dockerfile: apps/hub/Dockerfile
    image: tainnel-hub:dev
    env_file: .env
    ports:
      - "3030:3030"
      - "9090:9090"
    volumes:
      - hub-data:/repo/apps/hub/data
    restart: unless-stopped

volumes:
  hub-data: {}
